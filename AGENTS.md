# 交管法规案例推演助手 - 项目规范

## 项目概述

本项目是专为**中国人民公安大学交通管理工程专业新生**设计的AI辅助学习工具，旨在帮助用户理解交通事故责任认定的**逻辑和法规依据**。

### 核心功能

1. **案例分析**：通过结构化输入交通事故信息，系统依据法规进行逻辑推演，输出责任认定参考
2. **法规检索**：收录《道路交通安全法》及实施条例核心条款，支持关键词搜索
3. **流式输出**：采用SSE协议，实现打字机式分析过程展示

## 技术栈

- **框架**：Next.js 16 (App Router)
- **核心**：React 19
- **语言**：TypeScript 5
- **UI组件**：shadcn/ui (基于 Radix UI)
- **样式**：Tailwind CSS 4
- **包管理**：pnpm

## 项目结构

```
src/
├── app/
│   ├── api/
│   │   ├── analyze/route.ts    # 案例分析API（流式SSE输出）
│   │   └── regulations/route.ts # 法规知识库API
│   ├── globals.css
│   ├── layout.tsx               # 根布局
│   └── page.tsx                 # 主页面（案例分析与法规库）
├── components/ui/               # shadcn/ui组件库
├── lib/
│   ├── accident-analysis.ts     # 事故分析推理引擎
│   ├── traffic-regulations.ts   # 交管法规知识库（26条核心条款）
│   └── utils.ts                # 工具函数
```

## 核心模块

### 1. 法规知识库 (`traffic-regulations.ts`)

- 收录26条核心交管法规条款
- 按分类组织：信号灯违规、路权优先、变更车道、行人保护等12个类别
- 支持关键词检索和分类查询

**主要函数**：
- `searchRegulations(keywords)` - 关键词检索
- `getRegulationsByCategory(category)` - 分类查询
- `getAllCategories()` - 获取所有分类统计

### 2. 分析推理引擎 (`accident-analysis.ts`)

严格按照 **"引用法规 → 分析事实 → 逻辑推演 → 输出结论"** 流程执行分析：

**步骤1 - 法规检索**：提取事故关键词，检索相关法规条款
**步骤2 - 事实分析**：提取当事方信息、道路环境条件
**步骤3 - 逻辑推演**：根据规则判定各方过错
**步骤4 - 责任认定**：分配责任比例

## 转弯与直行事故责任认定规则

### 核心原则（必须严格遵守）

> 转弯车辆未让行直行车辆，是导致事故的直接原因。直行车辆有绝对优先通行权。

### 责任划分标准（唯一答案）

| 情形 | 责任划分 | 说明 |
| :--- | :--- | :--- |
| 转弯车未让行直行车（无其他过错） | **转弯车全责，直行车无责** | 这是唯一正确答案 |
| 转弯车未让行 + 直行车有明显违章（如超速、闯红灯） | 转弯车主责，直行车次责 | 仍需以转弯车为主要责任方 |
| 双方同时违章且难以区分主次 | 同等责任 | 仅适用于极特殊情况，如双方均闯红灯 |

### 禁止行为

- **禁止**将"转弯未让直行"判定为同等责任，除非直行车存在严重违章且与事故发生有直接因果关系
- **禁止**使用"双方都有注意义务"等模糊表述来回避明确责任划分

### 法条依据

- 《道路交通安全法实施条例》第五十二条第三项：**转弯的机动车让直行的车辆先行**

## 核心规则体系

### 1. 信号灯通行规则
- 红灯停、绿灯行
- 违反信号灯规定 → 全责

### 2. 路口让行规则
- 让优先通行方先行
- 无标志标线时，停车瞭望，让右方来车先行
- **转弯让直行**（转弯车全责）

### 3. 人行横道让行规则
- 机动车遇行人通过人行横道，应当停车让行

### 4. 追尾责任判定
- 后车未与前车保持安全距离 → 后车全责

### 5. 变更车道责任
- 变更车道时影响相关车道内行驶的机动车 → 变道车承担责任

### 6. 严重违章责任
- 酒驾、醉驾、毒驾 → 全责
- 逆向行驶 → 全责
- 超速行驶（严重违章）→ 可能减轻对方责任，但主责方仍为违章方

## 公路技术等级分类标准（依据JTG B01-2014）

### 等级划分总览
根据《公路工程技术标准》，公路分为五个技术等级：高速公路、一级公路、二级公路、三级公路、四级公路。

| 等级 | 核心特征 | 设计速度 | 适用场景 |
| :--- | :--- | :--- | :--- |
| 高速公路 | 专供汽车分方向分车道行驶、全部控制出入 | 80-120 km/h | 国家干线、省际通道 |
| 一级公路 | 供汽车分方向分车道行驶，部分控制出入 | 60-100 km/h | 连接重要政治经济中心 |
| 二级公路 | 双向两车道，不设中央分隔带 | 40-80 km/h | 连接县、乡主要交通 |
| 三级公路 | 双车道，通行能力较低 | 30-60 km/h | 连接乡镇、一般干线 |
| 四级公路 | 单车道或双车道，通行能力最低 | 20-40 km/h | 乡村道路、支线交通 |

### 技术等级对通行规则的影响
- 不同等级道路的**限速标准**不同（参见《道路交通安全法》第四十二条）
- 不同等级道路的**路权规则**可能存在差异（如高速公路禁止掉头、倒车）
- 事故责任认定中，道路等级是判断"是否合理使用道路"的重要参考因素

### 行政等级与技术等级的关系
公路按行政等级分为：国道、省道、县道、乡道。每个行政等级的道路都有对应的技术等级要求。新建公路应当符合技术等级要求。

## 道路等级处理规则

当用户描述的案例中涉及道路类型或等级时，系统必须：

1. **识别并标注道路的技术等级**（高速公路/一级/二级/三级/四级）
2. **基于该等级道路的限速标准判断是否存在超速行为**
3. **在责任推演中，将道路等级作为分析路权归属的参考因素之一**

### 输出格式要求
- 必须包含道路等级信息："事故发生在[等级]道路上，该等级道路限速范围为[XX-XX km/h]"
- 如用户未明确道路等级，主动提示用户补充

## 可视化示意图生成规则

当用户描述交通事故案例时，系统必须在回答中包含以下两部分：

### 第一部分：文字推演分析（原有内容保持不变）
### 第二部分：HTML示意图代码

生成一段完整的HTML代码，包含Canvas或SVG绘图：

**画布要求**：600×400像素

**绘制内容必须与用户描述的案例一致，至少包含**：
- 道路（双向车道，标明车道方向）
- 车辆位置（用矩形或简单车辆图标表示，标注A车/B车）
- 行驶方向（用箭头标注）
- 碰撞点（用红叉或特殊标记标注）

**颜色规范**：
- A车：绿色 (#22c55e)
- B车：红色 (#ef4444)
- 碰撞点：红色 (#dc2626)
- 道路：灰色 (#94a3b8)
- 道路标线：白色虚线

### 代码质量要求
- 使用HTML5标准
- 所有绘图逻辑写在`<canvas>`或`<svg>`标签内
- 添加必要的CSS样式使展示清晰
- 在Canvas上添加文字标注（如"A车（转弯）"、"B车（直行）"、"碰撞点"）
- 确保画布在不同分辨率下可缩放查看

### 交通场景绘图模板参考

**标准十字路口场景**：
- 画布中心绘制十字交叉道路，主路横向、支路纵向
- 道路内部画白色虚线表示车道分隔线
- 转弯车辆用绿色矩形表示，标注"A车"
- 直行车辆用红色矩形表示，标注"B车"
- 用红色×标注碰撞点位置（十字路口中央偏右转轨迹处）
- 用虚线弧线标注转弯车辆的预期转弯轨迹
- 用实线箭头标注直行车辆行驶方向

**自定义规则**：
- 车辆位置：根据用户描述动态调整（支路出发/主路出发等）
- 道路类型：根据道路等级调整（高速公路需画中央隔离带，乡村道路可不画中央线）
- 多车道：用多条平行虚线表示同向多车道

## API接口

**POST /api/analyze**
- 输入：事故场景JSON
- 输出：SSE流式分析过程
- 返回数据：法规检索 → 事实分析 → 逻辑推演 → 责任认定 → 学习要点

**GET /api/regulations**
- 查询参数：`action`（search/category/categories）、`keyword`
- 返回：法规列表或分类统计

## 运行命令

```bash
# 安装依赖
pnpm install

# 开发环境
pnpm dev

# 构建生产版本
pnpm build

# 生产环境启动
pnpm start
```

## 关键约束

1. **分析流程**：必须严格遵循"引用法规 → 分析事实 → 逻辑推演 → 输出结论"流程
2. **免责声明**：每次分析结果必须附带"仅供学习参考，不具备法律效力"的声明
3. **法规优先**：所有判断必须基于知识库中的法规条款，严禁凭空捏造
4. **流式输出**：涉及LLM调用的功能必须采用SSE流式输出
5. **唯一答案**：转弯与直行事故必须给出明确的责任划分
6. **方向信息强制收集**：涉及交叉路口的案例必须收集方向信息（行驶方向、停车原因、相对位置），缺失时必须先提问

## 方向信息强制收集规则

### 适用场景
当事故道路类型为"有信号灯交叉路口"或"无信号灯交叉路口"时，系统必须收集以下方向信息：

### 必填字段

| 字段名 | 说明 | 选项 |
|--------|------|------|
| 行驶方向 | 车辆的行驶方向 | 南北/北南/东西/西东 |
| 停车原因 | 停车等待的原因 | 红灯/让行/拥堵/故障 |
| 相对位置 | A车与B车的相对位置 | 左侧/右侧/对面 |

### 验证流程
1. **自动检测**：系统自动识别交叉路口类型
2. **前置验证**：分析前验证所有当事方的方向信息完整性
3. **缺失提示**：若方向信息缺失，返回提示问题列表，要求用户补充
4. **继续条件**：只有方向信息完整后，才进入正式分析流程

### UI交互
- 当道路类型选择为交叉路口时，前端表单自动显示方向信息输入字段
- 分析结果显示区域显示方向信息确认卡片，列出缺失信息和待回答问题
- 提示用户返回表单补充信息

## 适用场景

- 交通事故责任认定学习
- 交通法规条款理解
- 案例分析能力训练

## 限制说明

- 本工具为教学辅助工具，分析结论不具备法律效力
- 最终责任认定请以交管部门正式出具的事故认定书为准
